#include <gmock/gmock.h>
#include <gtest/gtest.h>

#include "bits/stdc++.h"

using namespace std;

class Solution {
 public:
  int solve() { return 0; }
};

TEST(SolutionTest, Test1) {
  Solution solution;
  auto actual_sol = solution.solve();
  EXPECT_EQ(actual_sol, 0);
}

TEST(SolutionTest, Test2) {
  Solution solution;
  auto actual_sol = solution.solve();
  EXPECT_EQ(actual_sol, 0);
}

TEST(SolutionTest, Test3) {
  Solution solution;
  auto actual_sol = solution.solve();
  EXPECT_EQ(actual_sol, 0);
}
